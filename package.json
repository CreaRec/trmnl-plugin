{
  "name": "@crearec/trmnl-plugin",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Live fridge dashboard JSON for TRMNL Private Plugin (weather, waste, calendar)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.5",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "luxon": "^3.7.2",
    "node-ical": "^0.27.1"
  }
}
